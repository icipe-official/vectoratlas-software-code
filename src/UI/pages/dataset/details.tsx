import { Container } from '@mui/material';
import React from 'react';
import { UploadedDatasetDetails } from '../../components/dataset/uploadedDatasetDetails';

const DatasetDetailsPage = () => {
  return (
    <Container>
      <UploadedDatasetDetails />
    </Container>
  );
};

export default DatasetDetailsPage;
